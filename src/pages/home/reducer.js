
const INITIAL_STATE = {
    nome: '',
    dataNascimento: '',
    email: '',
    confirmaEmail: '',
    senha: '',
    confirmarSenha: ''
}

export default (state = INITIAL_STATE, action) => {
    switch(action.type) {
        default:
            return state
    }
}